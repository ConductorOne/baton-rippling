package client

const (
	// TODO: update to use real base url
	// BaseURL    = "https://rest.ripplingapis.com"
	BaseURL    = "http://localhost:8080"
	UsersURL   = BaseURL + "/users"
	TeamsURL   = BaseURL + "/teams"
	WorkersURL = BaseURL + "/workers"
)
